# V1.23 Final2 engineering pass

- Removed Eight Precepts from Home counters while preserving its own screen and records.
- Home refresh no longer fires redundantly from both LaunchedEffect and lifecycle resume.
- Fixed today-range calculation for Five Aggregates / Dependent Origination observations.
- Unified Home core practice totals at the domain layer: meditation + walking + root-protection count; meditation + walking duration.
- Meditation completion still creates exactly one session automatically; “记录这一刻” only updates that session.
- Replaced the fixed completion note area with a keyboard-safe, scrollable Material 3 bottom sheet.
- Observation recent records now support single/multiple selection and confirmed deletion for all three observation types.
- Practice tab is reset to Anapanasati when the Practice destination is recreated.
- Bell remains local (`res/raw/soft_bell.wav`) with no INTERNET permission.

Build note: local Gradle compilation could not run in the sandbox because services.gradle.org and the Android plugin repository were not resolvable. V1.24 continues the offline application design; the final APK still needs an Android/Gradle environment with cached dependencies.
